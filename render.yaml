# Render Blueprint for GitHub Analyzer Backend

services:
  - type: web
    name: github-analyzer-api
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
      - key: GITHUB_TOKEN
        sync: false
